# Pawkit Pet Configuration
# Controls the pet's appearance and behavior.
# See docs/CONFIG.md for full reference.
#
# Config location: %APPDATA%/pawkit/pet.yaml

pet:
  sprite: "pixel-cat"
  scale: 2
  idle_timeout: 300
  start_position: "bottom-right"
  opacity: 1.0
  click_through: false
